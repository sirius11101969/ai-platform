import { crmActivityNavigation } from "../foundation";

export const crmActivitiesWorkspaceNavigation = Object.freeze({
  id: "crm.activities.tasks.workspace.navigation",
  label: crmActivityNavigation.label,
  route: crmActivityNavigation.route,
  section: "CRM",
  activeKey: "activities",
  source: "crm.activities.tasks.navigation",
});
