name: AS6 Backend Codex Agent

on:
  workflow_dispatch:
    inputs:
      task:
        description: "Backend task for AS6 Codex Agent"
        required: true
        type: string

permissions:
  contents: read

jobs:
  backend-agent:
    name: Backend Agent
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Run Backend Codex Agent
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt-file: .github/codex/prompts/as6-backend-agent.md
          sandbox: read-only
          safety-strategy: drop-sudo
          codex-args: '["--ephemeral"]'
