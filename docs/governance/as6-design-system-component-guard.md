# AS6 Design System Component Guard

STATUS=ACTIVE
RULE=AS6_DESIGN_SYSTEM_COMPONENT_GUARD

Any new reusable UI component must:

- use AS6 Design Tokens;
- avoid CRM business logic;
- expose a simple API;
- avoid duplicating existing shared components;
- be suitable for reuse across modules.
