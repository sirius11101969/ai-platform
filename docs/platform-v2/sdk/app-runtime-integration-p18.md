# AS6 Platform V2 — App Runtime Integration P18

## Purpose

P18 exposes dynamic shell runtime routes and navigation at the App integration layer.

## Includes

- App runtime hook
- App runtime navigation component
- App runtime route outlet
- App runtime integration panel

## Target behavior

Marketplace and future plugin pages can be rendered from dynamic route metadata without manually wiring each module.
