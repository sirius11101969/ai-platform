# AS6 Root Cause Governance Auto-Sync Runbook

Rule:

If root-cause router/remediation/validation pass but governance registry/coverage fail, auto-sync registry/coverage/state metadata before touching production.

Production policy:

- Production touched: NO
- Human approval required for production-changing actions: YES

Secrets:

Never print secret values. Use <INSERT_SECRET_HERE> only as a placeholder.
