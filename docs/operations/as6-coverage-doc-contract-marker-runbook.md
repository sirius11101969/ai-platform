# AS6 Coverage Doc Contract Marker Runbook

Rule:

Every docs/coverage/*.md file must contain at least one accepted coverage contract marker.

Secrets:

Never print secret values. Use <INSERT_SECRET_HERE> only as a placeholder.
