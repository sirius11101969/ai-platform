# AS6 Runtime Binary Empty Tmp Runbook

Rule:
Empty /tmp/as6-* files are runtime sentinels and must be SKIP, not FAIL.

Secrets:
Never print secret values. Use <INSERT_SECRET_HERE> only as a placeholder.
