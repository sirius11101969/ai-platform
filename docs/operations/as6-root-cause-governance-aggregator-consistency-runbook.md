# AS6 Root Cause Governance Aggregator Consistency Runbook

Rule:

Final governance FAIL must have at least one child FAIL in diagnostic, registry, coverage, prevention, or rollback dimensions.

Secrets:

Never print secret values. Use <INSERT_SECRET_HERE> only as a placeholder.
