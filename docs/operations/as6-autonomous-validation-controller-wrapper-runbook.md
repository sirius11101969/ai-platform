# AS6 Autonomous Validation Controller Wrapper Runbook

Rule:

If diagnose-all references an autonomous controller, the executable path must exist. If canonical implementation is diagnostic-prefixed, create a compatibility wrapper.

Secrets:

Never print secret values. Use <INSERT_SECRET_HERE> only as a placeholder.
