# AS6 Root Cause Registry Auto-Sync Runbook

Rule:

When root-cause router/remediation pass but registry/coverage fail, auto-sync metadata artifacts before touching production.

Secrets:

Never print secret values. Use <INSERT_SECRET_HERE> only as a placeholder.
