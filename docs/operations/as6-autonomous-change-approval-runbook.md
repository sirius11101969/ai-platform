# AS6 Autonomous Change Approval Operations

Rule:

Change Approval validates gates and creates approval evidence. It never applies production changes automatically.

Required result:

- AS6_CHANGE_APPROVAL_RESULT=OK

Secrets:

Never print secret values. Use only:

<INSERT_SECRET_HERE>
