# AS6 Validation

Before commit always run:
- frontend production build;
- frontend docker build when affected;
- Architecture Guardian;
- Secret Scan;
- syntax checks when applicable.

Never bypass failing validation.
Never disable tests/checks to make a commit pass.
