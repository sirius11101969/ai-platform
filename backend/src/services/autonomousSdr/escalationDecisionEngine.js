function recommendEscalation(input={}){ const reasons=[]; if(Number(input.estimatedRevenue||0)>=50000) reasons.push('high_value_lead'); if(Number(input.enterpriseSignals||0)>=2) reasons.push('enterprise_signals'); if(String(input.sentiment||'').toLowerCase()==='negative') reasons.push('negative_sentiment'); if(Number(input.objectionSeverity||0)>=2) reasons.push('complex_objections'); if(String(input.buyingIntent||'').includes('technical')) reasons.push('technical_requirements'); if(input.objectionCategory==='price_objection') reasons.push('high_pricing_sensitivity'); return { recommend: reasons.length>0, reasons } }
module.exports={ recommendEscalation }
