# AS6 Rendered Asset Path AEC V48

- Block closure if /assets/*.png appears as visible JSX text.
- Block closure if hashed production asset names appear in CommandCenterPage.jsx.
- Block closure if robot asset variable is used as a text child.
- Block closure if build or production health fails.
