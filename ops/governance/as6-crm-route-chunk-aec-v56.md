# AS6 CRM Route Chunk AEC V56

- Block closure if CRMPage is statically imported by App.jsx.
- Block closure if AiWorkersPage is statically imported by App.jsx.
- Block closure if CRM or AI Workers manual chunks are missing.
- Block closure if build does not emit crm-page and ai-workers-page chunks.
- Block closure if build or production health fails.
