# AS6 V116B Change Plan

1. Clean stale V115 artifacts.
2. Keep Command Center fix outside App.jsx.
3. Add source-level CSS lock for real Command Center classes.
4. Import CSS through existing command-center classic restore entry.
5. Fix visual drift: logo frame, sidebar frame, cyan action strip, Copilot outline, bottom line, lower overflow.
6. Add diagnostics, controls, failure classes and registry coverage.
7. Build frontend, deploy dist to nginx, validate, secret scan, commit and push.
