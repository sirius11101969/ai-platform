# AS6 Black Screen React Import AEC V57

- Block closure if lazy() is used without importing lazy from React.
- Block closure if Suspense is used without importing Suspense from React.
- Block closure if frontend build fails.
- Block closure if production health fails.
