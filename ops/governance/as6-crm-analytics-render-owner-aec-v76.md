# AS6 CRM Analytics Render Owner AEC V76
- Block closure if CRMAnalyticsPanel does not declare and call renderRevenuePanel.
- Block closure if CRMPage still uses a closing CRMAnalyticsPanel children bridge after V76.
- Block closure if CRMAnalyticsPanel adds visible wrapper markup.
- Block closure if build, route chunks, production health, or explicit scan fails.
- Block closure if runtime artifacts are staged.
