# AS6 V89 AEC

Every major page must expose global command access without page-specific duplication.
Command Palette must be mounted once and must support keyboard access.
Command actions must remain discoverable, compact and brand-aligned.
Build, production health, enforcement guard, scan and runtime staging guard must pass before commit and push.
