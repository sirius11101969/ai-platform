# AS6 Primary Fail Repair Policy

Rules:

- diagnose-all must not contain unreachable diagnostic insertion blocks after top-level exit.
- autonomous coverage must have explicit diagnostic, coverage, registry, prevention, rollback, evidence and AEC linkage.
- Cascading controller failures must be fixed at the primary root cause, not by weakening downstream gates.
