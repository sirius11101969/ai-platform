# AS6 V83 AEC

Before commit/push, a single enforcement workflow must validate registry enforcement, readiness baseline, secret scan, runtime staging and production health.
New diagnostics/controls must remain registered and covered.
Runtime artifacts must never be staged.
Secrets, tokens, keys and passwords must never be printed or committed.
