# AS6 Copilot Inline Layout AEC V40
- Block closure if Copilot description text remains.
- Block closure if copilot-inline-layout is missing.
- Block closure if as6-copilot-inline-logo is missing.
- Block closure if CTA is missing.
- Block closure if build or health fails.
