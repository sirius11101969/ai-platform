# AS6 V95 AEC

All AS6 pages must visually follow the Command Center brand language.
Shared UI primitives and brand tokens must be used instead of local visual drift.
Cards, KPIs, tables, forms, buttons, charts, sidebar and topbar must remain aligned with AS6 Mission Control.
Build, production health, enforcement guard, scan and runtime staging guard must pass before commit and push.
