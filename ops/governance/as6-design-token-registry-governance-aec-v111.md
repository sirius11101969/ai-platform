# AS6 V111 AEC

All new UI must use canonical AS6 design tokens.
Spacing, radius, shadows, typography, KPI, table and status badge tokens must not drift.
Build, production health, enforcement guard, scan and runtime staging guard must pass before commit and push.
