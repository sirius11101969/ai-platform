# AS6 V82 AEC

Any new ops/bin/as6-diagnose-* or ops/bin/as6-control-* artifact must have registry evidence before commit.
Any new enforcement failure class must be present in registry, governance, state and detected errors before closure.
Runtime artifacts must never be staged.
Production health must pass before commit and push.
