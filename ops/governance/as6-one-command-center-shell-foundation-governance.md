# Governance

AS6 ONE preview must use the existing Command Center shell foundation and must not introduce a second competing shell. `/crm`, `/crm-v2`, and `/command-center` page implementations are protected from visual rewrites in this task.
