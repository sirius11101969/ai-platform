# AS6 V120 AEC

/command-center must render as AppShell workspace content only.
CommandCenterPage.jsx must not include its own sidebar, logo panel or duplicate navigation.
The existing global AppShell sidebar remains the single navigation source.
