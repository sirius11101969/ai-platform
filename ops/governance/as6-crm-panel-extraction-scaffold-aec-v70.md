# AS6 CRM Panel Extraction Scaffold AEC V70
- Block CRM panel wiring if scaffold modules are missing.
- Block closure if scaffold modules render visible UI before extraction.
- Block closure if CRM chunk, Command Center chunk, build or health validation fails.
