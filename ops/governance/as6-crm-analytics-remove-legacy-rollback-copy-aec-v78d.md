# AS6 V78D AEC

Control must not pass an external image name to docker compose run as a service.
Control must use host npm or docker run image fallback.
Production health must pass after build validation.
