# AS6 Screen 1 Refinement v2 — AEC rules

- A later CSS layer must not silently shrink the primary company logo or enlarge the profile avatar beyond the registered hierarchy.
- Dark mode must remain neutral black; blue field fills and warm/yellow central glows are prohibited.
- The intent surface must have no persistent container outline; its outline is allowed only through `:focus-within`.
- Workspace read responses must be ordered so stale requests cannot restore a previous company.
- Locale controls must provide immediate visual feedback.
- A company named AS6 must not expose a redundant `AS6 + AS6` branding choice.
- Plan entry copy must describe both plan management and available upgrades.
