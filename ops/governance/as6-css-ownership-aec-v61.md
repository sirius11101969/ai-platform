# AS6 CSS Ownership AEC V61

- Block closure if CSS ownership diagnostics are missing.
- Block broad CSS deletion unless selector ownership evidence exists.
- Block closure if AS6 branding/Copilot/robot CSS contracts are removed.
- Block closure if duplicate top Command Center logo returns.
- Block closure if CSS bundle exceeds budget.
- Block closure if build or production health fails.
