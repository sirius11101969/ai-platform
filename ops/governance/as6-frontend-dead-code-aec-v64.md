# AS6 Frontend Dead Code AEC V64

- Block dead-code deletion if ownership diagnostics are missing.
- Block cleanup if probable unused pages/components/CSS reports are missing.
- Block cleanup if route chunks regress.
- Block cleanup if frontend build or production health fails.
