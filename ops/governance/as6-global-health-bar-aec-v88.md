# AS6 V88 AEC

Every major page must receive global health context without page-specific duplication.
Global Health Bar must remain mounted once and must not block page interaction.
Mobile layout must preserve compact platform status visibility.
Build, production health, enforcement guard, scan and runtime staging guard must pass before commit and push.
