# AS6 Command Center JSX Object Integrity Control V31
- Regex/autopatch changes to JSX object literals must be followed by JSX integrity diagnostics.
- Asset variables must not drift between import name and usage name.
- KPI icon JSX must remain a single valid object property value.
- Docker BuildKit overlay corruption must use legacy build fallback for validation until daemon cache is repaired.
- Production health must remain OK after compose up.
