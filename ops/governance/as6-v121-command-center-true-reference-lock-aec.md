# AS6 V121 AEC

/command-center must render inside the existing AppShell without a duplicate sidebar.
External body-level AS6 overlay roots must be hidden by exact ID while CommandCenterPage is mounted.
Overlay root suppression must restore previous styles on unmount.
