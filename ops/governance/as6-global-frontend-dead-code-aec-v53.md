# AS6 Global Frontend Dead Code AEC V53
- Block closure if extra top logo render remains.
- Block closure if current Copilot or robot icon CSS contracts are removed.
- Block closure if build emits CSS syntax errors.
- Block closure if production health fails.
