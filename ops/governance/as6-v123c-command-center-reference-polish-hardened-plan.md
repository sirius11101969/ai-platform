# AS6 V123C Change Plan

1. Keep real CommandCenterPage.jsx from V122B.
2. Harden route-aware guard.
3. Add scoped reference polish CSS.
4. Suppress legacy sidebar labels: Executive Command, Control Tower 100%, Operations Timeline, Events.
5. Remove logo frame drift, action strip drift, Copilot/card outline drift and bottom noise drift.
6. Use git add -A only to avoid missing pathspec failures.
7. Build, deploy, validate, secret scan, commit and push.
