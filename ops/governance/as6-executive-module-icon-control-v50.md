# AS6 Executive Module Icon Control V50

- Executive module icons must not use imported asset URL strings as text values.
- Imported PNG values in module icon fields must be rendered through img JSX.
- Hashed /assets/*.png paths must never appear as visible UI text.
- Build and production health validation are mandatory.
