# AS6 Vite Config Syntax Repair Governance

- Rule: config files must not be patched with unsafe partial regex replacements.
- Rule: Vite manualChunks must remain syntactically valid and build-verified.
- Rule: build warning fixes must validate config parsing before commit.
