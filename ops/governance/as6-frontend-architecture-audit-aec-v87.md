# AS6 V87 AEC

Before new component UI work starts, frontend architecture inventory must exist.
New UI stages must add reusable components rather than blind CSS-over-CSS overlays.
Every new UI component must be registered in diagnostics, coverage, governance, state and detected errors.
Build, production health, enforcement guard, secret scan and runtime staging guard must pass before commit and push.
