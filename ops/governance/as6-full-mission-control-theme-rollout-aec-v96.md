# AS6 V96 AEC

All pages must inherit the Command Center brand style through the V96 rollout layer.
Legacy layouts, cards, tables, forms, page shells and local visual systems must not override AS6 Mission Control brand language.
Build, production health, enforcement guard, scan and runtime staging guard must pass before commit and push.
