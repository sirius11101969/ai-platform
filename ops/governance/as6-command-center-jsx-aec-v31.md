# AS6 Command Center JSX AEC Rules V31
- Block closure if CommandCenterPage.jsx contains as6RobotAsset without import.
- Block closure if KPI icon JSX spans into a second sibling node without an object property key.
- Block closure if Vite build fails.
- Block closure if diagnostic registry and coverage registry are not updated.
- Block closure if production health is not OK after deploy.
