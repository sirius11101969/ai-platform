# AS6 CSS Dead Selectors Control V65

- CSS selectors must not be deleted unless V64/V65 ownership diagnostics classify them as safe.
- Protected interface selectors must never be treated as dead.
- Legacy Command Center CSS blocks must be mapped before cleanup.
- Route splitting, WebP branding, duplicate logo guards, build validation, and production health are mandatory.
