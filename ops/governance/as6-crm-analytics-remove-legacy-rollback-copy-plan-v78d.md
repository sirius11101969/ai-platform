# AS6 V78D Change Plan

1. Preserve V78/V78B/V78C diagnostic fixes.
2. Replace invalid Docker Compose image invocation with docker run image fallback.
3. Register DOCKER_COMPOSE_IMAGE_AS_SERVICE_BUILD_RUNNER_GAP.
4. Validate diagnostics, build, health, secret scan, commit and push.
