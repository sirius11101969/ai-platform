# AS6 Frontend Budgets AEC V58

- Block closure if main JS exceeds 220 KB.
- Block closure if CSS bundle exceeds 180 KB.
- Block closure if Command Center chunk exceeds 30 KB.
- Block closure if CRM chunk exceeds 120 KB.
- Block closure if AI Workers chunk exceeds 65 KB.
- Block closure if duplicate Command Center top logo returns.
- Block closure if CRM or AI Workers lazy loading regresses.
- Block closure if build or production health fails.
