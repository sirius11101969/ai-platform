# AS6 V114B AEC

UI shell rollout is incomplete unless production container/static bundle is rebuilt and visible.
Legacy sidebar must be suppressed on non-command-center pages.
Browser/cache stale bundle must be treated as production visual drift.
