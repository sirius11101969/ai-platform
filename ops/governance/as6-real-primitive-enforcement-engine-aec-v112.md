# AS6 V112 AEC

All KPI, card, table, filter, action bar, empty, loading and error UI must use AS6 unified primitives.
Local primitive implementations must be detected, classified and migrated.
Build, production health, enforcement guard, scan and runtime staging guard must pass before commit and push.
