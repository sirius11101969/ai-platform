# AS6 Selector Ownership Map AEC V62

- Block CSS deletion if selector ownership map is missing.
- Block closure if protected contract selectors are marked dead.
- Block broad CSS cleanup unless selector ownership and screenshot validation exist.
- Block closure if frontend build or production health fails.
