# AS6 CSS Dead Selectors AEC V65

- Block selector deletion if V65 ownership diagnostics are missing.
- Block closure if protected selectors are marked dead.
- Block closure if legacy Command Center CSS map is missing.
- Block closure if build, route chunks, or production health fail.
