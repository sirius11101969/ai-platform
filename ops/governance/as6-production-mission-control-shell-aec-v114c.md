# AS6 V114C AEC

Visible shell migration must be mounted from frontend main entry.
Production static bundle must be copied directly into nginx serving directory when compose build is unavailable.
Legacy AI-OS shell visibility after deploy is a release blocker.
