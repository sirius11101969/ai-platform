# AS6 Repair V76 Terminal Paste Integrity AEC
- Block closure if markdown fence metadata is pasted into shell and not registered.
- Block closure if V76 render-owner shape is not verified after partial push.
- Block closure if malformed V76 artifacts remain untracked outside runtime quarantine.
- Block closure if build, route chunks, production health, or explicit scan fails.
- Block closure if runtime artifacts are staged.
