# AS6 Rendered Asset Path Control V48

- Asset module values must never be rendered as visible text.
- PNG asset paths must only be used as img src values or CSS url values.
- Hashed production asset filenames must never appear as UI text.
- Build and production health validation are mandatory.
