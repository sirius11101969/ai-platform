# AS6 Frontend Bundle AEC V35

- Block closure if frontend build fails.
- Block closure if bundle diagnostics are missing.
- Register bundle-size drift into coverage/governance/state.
- grep diagnostics under pipefail must not abort without explicit handling.
