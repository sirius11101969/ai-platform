#!/usr/bin/env bash
set -euo pipefail

ROOT="${AS6_REPO_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}"
cd "$ROOT"

ops/bin/as6-update-diagnostic-status-registry >/dev/null

echo "DIAGNOSTIC_STATUS_REGISTRY_REFRESHED=ops/status/diagnostic-status-registry.json"
echo "DIAGNOSTIC_STATUS_REGISTRY_COUNT=$(python3 - <<'PY'
import json
print(len(json.load(open("ops/status/diagnostic-status-registry.json")).get("diagnostics", [])))
PY
)"
echo "DIAGNOSTIC_STATUS_REGISTRY_REFRESH_RESULT=OK"
